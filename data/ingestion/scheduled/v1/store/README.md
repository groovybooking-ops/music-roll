# Shared scheduled queue

Future scheduled discovery occurrences, candidate snapshots, append-only candidate events, provider caches, and rebuildable indexes share this store. Stage 1 intentionally leaves it empty of candidates and provider evidence.
